# Root deps (backend builds its own)
node_modules

# Git / secrets / logs
.git
.env
.env.*
*.log
.DS_Store

# Next.js web app — built in its own container, not needed here
ui/web/node_modules
ui/web/.next
ui/web/bun.lock
ui/web/package-lock.json

# WhatsApp auth state (mounted as a volume)
backend/auth_whatsapp
