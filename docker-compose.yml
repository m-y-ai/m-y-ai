services:
  m-y-ai:
    build: .
    env_file: .env
    ports:
      - "4096:4096"
    volumes:
      - wa-auth:/app/auth_whatsapp
      - memory:/home/claw/m-y-ai
    restart: unless-stopped

volumes:
  wa-auth:
  memory:
